db.getCollection('Tasks').aggregate(
  [
    {
      $lookup: {
        from: 'Tasks',
        localField: 'topicId',
        foreignField: 'topicId',
        as: 'taskInfo'
      }
    }
  ],
  { maxTimeMS: 60000, allowDiskUse: true }
);